{
  "name": "push-lokalise-keys",
  "version": "4.4.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:github": "ncc build index.js -o dist",
    "build:gitlab": "ncc build gitlab.js -o dist/gitlab",
    "build:all": "npm run build:github && npm run build:gitlab",
    "prepare": "npm run build:all"
  },
  "keywords": [],
  "author": "",
  "license": "No license. Source is copyrighted.",
  "dependencies": {
    "@actions/core": "^1.1.1",
    "@lokalise/node-api": "^6.2.1",
    "json-difference": "1.0.4",
    "node-fetch": "^3.3.2",
    "octokit": "^5.0.5",
    "properties-parser": "^0.3.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
